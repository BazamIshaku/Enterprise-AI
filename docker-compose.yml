services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: eunia
      POSTGRES_USER: eunia
      POSTGRES_PASSWORD: eunia-local-only
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
